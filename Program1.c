#include <stdio.h>
int main()
{
    // int a, b, c;
    // a = 10;
    // b = 20;
    // c = a + b;
    //  printf("Sum of a=%d and b=%d = %d", a, b, c);

    int a, b;
    a = 10;
    b = a % 3;
    printf("%d", b);

    return 0;
}